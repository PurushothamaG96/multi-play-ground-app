# docker-compose.prod.yml
services:
  app:
    container_name: nest-app-prod
    build:
      context: .
      target: production
    env_file:
      - .env.production
    ports:
      - '3000:3000'
    restart: always
